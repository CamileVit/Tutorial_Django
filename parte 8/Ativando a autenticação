INSTALLED_APPS = [
    ...
    'django.contrib.auth',  #Estrutura de autenticação #Core e seus modelos padrão.
    'django.contrib.contenttypes',  #Sistema de tipo de conteúdo #Django (permite que permissões sejam associadas a modelos).
    ....

MIDDLEWARE = [
    ...
    'django.contrib.sessions.middleware.SessionMiddleware',  #Gerencia sessões entre solicitações
    ...
    'django.contrib.auth.middleware.AuthenticationMiddleware',  ##Associa usuários a solicitações usando sessões
